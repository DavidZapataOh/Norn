{
  "name": "norn",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first invoice reconciliation with portable, verifiable evidence.",
  "license": "Apache-2.0",
  "engines": { "node": ">=22.17" },
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run dev -w @norn/site",
    "build": "npm run build -w @norn/site"
  }
}
